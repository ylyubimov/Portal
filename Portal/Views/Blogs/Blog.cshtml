@model Portal.Controllers.Blog

@{
    ViewBag.Title = "Blog";
}

<h2>Blog @Model.id</h2>

<ul>
    @foreach (Portal.Controllers.Article article in Model.articles) {
        <li>@Html.ActionLink(article.id.ToString(), "index", "articles", new { @id=article.id }, null)</li>
    }
    
</ul>

