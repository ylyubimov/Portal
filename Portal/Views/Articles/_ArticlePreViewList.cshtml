@if (Model.Length != 0)
{
    foreach (var i in Model)
    {
       @Html.Partial("../Articles/_ArticlePreView", (Portal.Models.Article)i)
    }
}
else
{
    <div class="container search-error-wrap">
        <div class="search-error">
            К сожалению, статей нет
        </div>
    </div>
}