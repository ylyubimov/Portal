@using Portal.Models
@model Portal.Models.Blog

<div class="container blog-wrap">
    <div class="blog-preview">
        <div class="author blog-author">
            <img class="img-circle" src=@Model.Author.Picture.URL />
            @Model.Author.First_Name @Model.Author.Second_Name
        </div>
        <h3 class="blog-title">@Html.ActionLink(@Model.Name, "blog", new { @id = Model.ID })</h3></a>
        <div class="blog-articles-length">@Model.Articles.ToArray().Length articles</div>
    </div>
</div>
